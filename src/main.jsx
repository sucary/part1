import ReactDOM from 'react-dom/client'

import App from './App'

//renders the content of the component to the div element defined by the file
//index.html with id value 'root'.
ReactDOM.createRoot(document.getElementById('root')).render(<App />)
